CVE-2026-42945 — KUDU / App Service
====================================

Comandos (en orden)
-------------------
apt-get update -qq && apt-get install -y git
cd /home
git clone https://github.com/pablinux87/nginx_check_CVE.git
cd /home/nginx_check_CVE
git pull
cd vm-local
chmod +x nginx-rift-*.sh

# Informe completo (adjuntar al mail platform):
./nginx-rift-report.sh -o /home/nginx-rift-reporte.txt

# O solo en pantalla:
./nginx-rift-report.sh

El informe incluye:
  1. Versión NGINX y si hay que actualizar (SÍ/NO)
  2. ASLR (qué es + estado + acción)
  3. Config rewrite (CORRECTA / CORREGIR + archivos y líneas)
  4. Logs (crashes signal 11)
  5. nginx -t
  6. Lista de acciones para ticket
  VEREDICTO_FINAL: OK | PARCHE_PLATAFORMA | URGENTE | REVISAR

Referencias: F5 K000161019 | nginx.org security advisories
