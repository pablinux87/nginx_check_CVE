CVE-2026-42945 — Guía Multiplica
==================================

FASE 1 — Desde tu PC (dominios públicos)
  Editar domains.txt → docker compose run --rm scanner
  → reporte-externo.csv (veredicto por dominio)

FASE 2 — Dentro de cada App Service (KUDU)
  apt-get update -qq && apt-get install -y git
  cd /home
  git clone https://github.com/pablinux87/nginx_check_CVE.git
  cd /home/nginx_check_CVE && git pull && cd vm-local
  chmod +x nginx-rift-*.sh
  ./nginx-rift-report.sh -o /home/nginx-rift-reporte.txt

  El informe cubre: versión, ASLR, rewrites, logs, acciones para ticket.
  Adjuntar nginx-rift-reporte.txt al mail de platform.

VEREDICTO_FINAL en KUDU:
  PARCHE_PLATAFORMA = config OK pero nginx < 1.30.1 (pedir parche a platform)
  URGENTE = versión + rewrites mal
  OK = todo bien en esa instancia
